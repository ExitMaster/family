{
  "name": "kkoongs",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8080",
    "deploy": "firebase deploy --only firestore:rules,hosting"
  }
}
