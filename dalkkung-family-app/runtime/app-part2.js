function icon(name){const common='viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"';const paths={home:'<path d="M3 11.5 12 4l9 7.5"/><path d="M5.5 10.5V20h13v-9.5"/><path d="M9.5 20v-6h5v6"/>',ledger:'<rect x="5" y="3" width="14" height="18" rx="2"/><path d="M8 7h8M8 11h8M8 15h5"/>',settlement:'<circle cx="12" cy="12" r="9"/><path d="M8.5 8.5 10 15l2-5 2 5 1.5-6.5"/>',care:'<path d="M20.8 4.6a5.5 5.5 0 0 0-7.8 0L12 5.7l-1.1-1.1a5.5 5.5 0 0 0-7.8 7.8l1.1 1.1L12 21l7.8-7.5 1.1-1.1a5.5 5.5 0 0 0-.1-7.8Z"/>',settings:'<circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.7 1.7 0 0 0 .3 1.9l.1.1-2.8 2.8-.1-.1a1.7 1.7 0 0 0-1.9-.3 1.7 1.7 0 0 0-1 1.6V21h-4v-.1a1.7 1.7 0 0 0-1-1.6 1.7 1.7 0 0 0-1.9.3l-.1.1L4.2 17l.1-.1a1.7 1.7 0 0 0 .3-1.9A1.7 1.7 0 0 0 3 14H3v-4h.1a1.7 1.7 0 0 0 1.6-1 1.7 1.7 0 0 0-.3-1.9L4.2 7 7 4.2l.1.1a1.7 1.7 0 0 0 1.9.3A1.7 1.7 0 0 0 10 3V3h4v.1a1.7 1.7 0 0 0 1 1.6 1.7 1.7 0 0 0 1.9-.3l.1-.1L19.8 7l-.1.1a1.7 1.7 0 0 0-.3 1.9 1.7 1.7 0 0 0 1.6 1h.1v4H21a1.7 1.7 0 0 0-1.6 1Z"/>',users:'<path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.9M16 3.1a4 4 0 0 1 0 7.8"/>',wallet:'<path d="M4 7h15a2 2 0 0 1 2 2v9a2 2 0 0 1-2 2H5a3 3 0 0 1-3-3V6a3 3 0 0 1 3-3h12v4"/><path d="M16 12h5v4h-5a2 2 0 0 1 0-4Z"/>',card:'<rect x="3" y="5" width="18" height="14" rx="2"/><path d="M3 10h18"/>',scale:'<path d="M12 3v18M5 6h14M5 6l-3 7h6L5 6ZM19 6l-3 7h6l-3-7ZM8 21h8"/>',money:'<rect x="3" y="6" width="18" height="12" rx="2"/><circle cx="12" cy="12" r="2.5"/><path d="M7 9H6v1M17 15h1v-1"/>'};return `<svg ${common}>${paths[name]||paths.home}</svg>`;}
function render(){const tabs=[['home','홈'],['ledger','장부'],['settlement','정산'],['care','간병'],['settings','설정']];$('#app').innerHTML=`<div class="shell"><div class="topbar"><div class="brand"><h1>${esc(state.family.name)}</h1><small>가족 공동 기록</small></div><div class="user-area"><div class="family-count">${icon('users')} ${state.members.length}/8명 공동 사용</div><div class="user-menu"><div class="user-pill"><strong>${esc(state.membership.display_name)}</strong><button id="signout" class="logout">로그아웃</button></div></div></div></div><nav class="nav">${tabs.map(([k,l])=>`<button data-tab="${k}" class="${state.tab===k?'active':''}">${icon(k)}<span>${l}</span></button>`).join('')}</nav><main id="main"></main></div>${state.modal?modalHTML():''}`;
  document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{state.tab=b.dataset.tab;state.modal=null;render();}); $('#signout').onclick=()=>signOut(state.auth); renderTab(); bindLinkButtons(); if(state.modal) bindModal();
}
function renderTab(){ if(state.tab==='home')home(); if(state.tab==='ledger')ledger(); if(state.tab==='settlement')settlement(); if(state.tab==='care')care(); if(state.tab==='settings')settings(); }
function title(t,sub,action=''){return `<div class="section-title"><div><h2>${t}</h2>${sub?`<p>${sub}</p>`:''}</div>${action}</div>`;}
function compactDate(d){if(!d)return '';const x=new Date(d+(String(d).length===10?'T00:00:00':''));return `${x.getMonth()+1}.${x.getDate()}`;}
function progressHTML(){const cycles=Array.from({length:12},(_,i)=>state.chemoCycles.find(c=>c.cycle_number===i+1)||{cycle_number:i+1});const completed=cycles.filter(c=>c.cycle_date).length;return `<div class="progress-card"><div class="progress-head"><strong>항암 진행 현황 <span class="muted">(1~12회차)</span></strong><button class="section-link" data-go-care>자세히 보기 ›</button></div><div class="cycle-track">${cycles.map((c,i)=>`<div class="cycle-dot ${c.cycle_date?'done':i===completed?'current':''}"><div class="cycle-bubble">${c.cycle_number}</div><div class="cycle-date">${c.cycle_date?compactDate(c.cycle_date):''}</div></div>`).join('')}</div><div class="progress-foot"><span class="done-label">${completed}회 완료</span><span>남은 ${12-completed}회 예정</span></div></div>`;}
function home(){const k=calc();const recent=state.careEvents.slice(0,2);$('#main').innerHTML=`<section class="hero"><div class="hero-copy"><div class="eyebrow">♥ 가족이 함께 만드는 기록</div><h2>함께 기록하며<br>엄마의 회복을 <em>응원해요</em></h2><p>장부, 정산, 치료와 간병 기록을 가족 8명이 한 곳에서 공유합니다.</p></div><div class="hero-art" aria-hidden="true"></div></section><div class="grid kpis"><div class="card kpi balance"><div class="label">현재 잔액</div><div class="value">${fmt(k.balance)}</div><div class="kpi-icon">${icon('wallet')}</div></div><div class="card kpi income-card"><div class="label">총 수입</div><div class="value income">${fmt(k.income)}</div><div class="kpi-icon">${icon('money')}</div></div><div class="card kpi expense-card"><div class="label">총 지출</div><div class="value expense">${fmt(k.expense)}</div><div class="kpi-icon">${icon('card')}</div></div><div class="card kpi settle-card"><div class="label">미정산</div><div class="value">${fmt(k.unpaid)}</div><div class="kpi-icon">${icon('scale')}</div></div></div><div class="quick-actions"><button class="quick-action" data-open="tx"><span class="plus">+</span>거래</button><button class="quick-action" data-open="care"><span class="plus">+</span>간병기록</button><button class="quick-action" data-open="health"><span class="plus">+</span>상태기록</button></div>${title('치료 진행','현재 항암 치료 일정을 한눈에 확인합니다.')} ${progressHTML()}${title('최근 간병 기록','가족이 최근 남긴 기록')}<div class="timeline compact-timeline">${recent.map(eventCard).join('')||'<div class="empty">기록이 없습니다.</div>'}</div>`; bindOpen();document.querySelectorAll('[data-go-care]').forEach(b=>b.onclick=()=>{state.tab='care';render();});}
function ledger(){const all=state.transactions;const rows=state.ledgerFilter==='all'?all:all.filter(x=>x.type===state.ledgerFilter);const total=rows.reduce((a,x)=>a+Number(x.amount),0);$('#main').innerHTML=`${title('장부','공동계좌의 수입과 지출 기록','<button class="btn" data-open="tx">+ 거래</button>')}<div class="filter-row"><div class="chips"><button class="chip ${state.ledgerFilter==='all'?'active':''}" data-filter="all">전체</button><button class="chip ${state.ledgerFilter==='income'?'active':''}" data-filter="income">수입</button><button class="chip ${state.ledgerFilter==='expense'?'active':''}" data-filter="expense">지출</button></div><div class="ledger-summary">${rows.length}건 · ${fmt(total)}</div></div><div class="list">${rows.map(x=>`<div class="item transaction"><div class="tx-body"><div class="tx-top"><span class="tx-kind ${x.type}">${x.type==='income'?'수입':'지출'}</span><span class="muted" style="font-size:11px">${dstr(x.txn_date)}</span></div><div class="row"><div class="tx-title">${esc(x.description)}</div><div class="money tx-amount ${x.type}">${x.type==='income'?'+':'-'} ${fmt(x.amount)}</div></div><div class="tx-tags"><span class="tag">⌂ ${esc(hname(x.household_id))}</span><span class="tag blue">${esc(x.payment_method||'')}</span><span class="tag">${esc(cname(x.category_id))}</span></div>${x.memo?`<p class="tx-memo">메모 · ${esc(x.memo)}</p>`:''}</div></div>`).join('')||'<div class="empty">거래가 없습니다.</div>'}</div>`;bindOpen();document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{state.ledgerFilter=b.dataset.filter;ledger();});}
function settlement(){const k=calc();const cards=state.settlements.map(s=>{const ss=state.shares.filter(x=>x.settlement_id===s.id);return `<div class="item settlement-card"><div class="settle-main"><div><div class="row"><strong>${esc(s.description)}</strong>${s.is_example?'<span class="badge warn">예시</span>':''}</div><div class="meta"><span>${dstr(s.settlement_date)}</span><span>결제 · ${esc(hname(s.payer_household_id))}</span></div><div class="total">${fmt(s.total_amount)}</div></div></div><div class="share-list">${ss.map(sh=>`<div class="share-row"><strong>${esc(hname(sh.household_id))}</strong><span class="money">${fmt(sh.amount)}</span><span class="status ${sh.status}">${statusLabel(sh.status)}</span></div>`).join('')}</div>${s.is_example?'<div class="notice" style="margin-top:10px">예시 항목입니다. 실제 정산 판단 시 구분해서 확인하세요.</div>':''}</div>`});$('#main').innerHTML=`${title('정산','가정 간 대납과 분담 현황','<button class="btn" data-open="settle">+ 정산</button>')}<div class="settlement-summary"><div><strong>가정 간 정산</strong><p>${k.unpaid===0?'모든 정산이 완료되었습니다.':'아직 완료되지 않은 분담금이 있습니다.'}</p></div><div><div class="muted" style="text-align:right;font-size:11px">미정산</div><div class="amount">${fmt(k.unpaid)}</div></div></div>${title('정산 내역','하영 · 하림 · 경수 3가정 기준')}<div class="list">${cards.join('')||'<div class="empty">정산 내역이 없습니다.</div>'}</div>`;bindOpen();}
